from flask import Blueprint

metrics = Blueprint('metrics', __name__)

from . import views

def sadf_headers(file):
    
def sadf(*kwargs):
    if data_type is None:
        data_type = "cpu"
    if sarfile is None:
        return -1
    cmd = "sadf -j "
    if data_type in ("cpu", "network):
        
